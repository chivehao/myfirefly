import type {DiaryConfig} from "@/types/diary.ts";

export const diaryConfig: DiaryConfig = {
    enable: false,
    utcTimeZone: 8,
    displayInHome: true,
    comment: true,
    add2Rss: false,
    displayInStatic: true,
}