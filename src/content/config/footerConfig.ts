import type { FooterConfig } from "../../types/config.ts";

export const footerConfig: FooterConfig = {
	// 是否启用Footer HTML注入功能
	enable: true,
	icp: "赣ICP备2025068655号-1",
};

// 直接编辑 config/FooterConfig.html 文件来添加备案号等自定义内容
